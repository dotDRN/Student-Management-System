# Optional. If unset, the app uses base URL `/api` and Vite proxies `/api` → http://localhost:5000 (see vite.config.ts).
# For production builds, set the full API base including /api prefix, e.g.:
# VITE_API_URL=https://your-api.example.com/api

VITE_API_URL=/api
